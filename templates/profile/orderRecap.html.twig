{% extends 'base.html.twig' %}

{% block title %}votre commandes{% endblock %}

{% block body %}
    <section class="user">
        <div class="row py-5">
            <div class="col-8 offset-2">
                {% for label, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="flash-{{ label }} bg-{{ label }} text-light p-3 mb-5 rounded">
                            {{ message }}
                        </div>
                    {% endfor %}
                {% endfor %}
            </div>
        </div>
        <h1>Votre commande</h1>
        {{ form_start(form)}}
            {{form_row(form.address, {'label':'Sélectionner votre adresse de livraison :'}) }}

        <table class="table align-middle text-center table-hover table-sm">
            <thead class="text-center table-secondary">
                <tr class="fw-bold">
                    <th>Produit</th>
                    <th>Quantité</th>
                    <th>Prix unitaire</th>
                    <th>Sous-total</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    {% for cart in carts %}
                        <td><img src="{{ asset('img/' ~ cart.product.img) }}" alt="photo principale {{ cart.product.name }}" class="me-3">
                            {{ cart.product.name }} {{ cart.product.types.name|default(' ') }} {{ cart.product.size.name|default(' ') }} {{ cart.product.color.name|default(' ') }} {{ cart.product.angle.name|default(' ') }} {{ cart.product.drilling.name|default(' ') }}
                        </td>
                        <td>{{ cart.quantity }}</td>
                        <td>{{ cart.product.price }} €</td>
                        <td>{{ (cart.product.price * cart.quantity)|number_format(0, ',', ' ') }} €</td>
                </tr>
                {% endfor %}
            </tbody>
                <tfoot class="table-secondary text-end">
                    <tr class="fw-bold">
                        <th colspan="3">TOTAL :</th>
                        <th>{{ total|number_format(0, ',', ' ') }} €</th>
                    </tr>
                </tfoot>
        </table>
        <div class="d-flex justify-content-end my-5">
            <a href="{{ path('profile_address_create') }}" class="btn btn-primary me-3">Ajouter une adresse</a>
            <button class="btn_shape btn-success btn-sm" type="submit">Payer la commande</button>
        </div>
        {{ form_end(form)}}
    </section>
{% endblock %}