{% extends 'baseAdmin.html.twig' %}

{% block title %}couleurs{% endblock %}

{% block body %}

    <h1>Liste des différentes couleurs</h1>

    <table class="table text-center table-hover table-sm">
        <thead>
            <tr>
                <th>N°</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for color in colors %}
                <tr class="align-middle">
                    <td>{{ loop.index }}</td>
                    <td>{{ color.name }}</td>
                     <td>
                        <a href="{{ path('color_update', {'id': color.id}) }}" class="text-primary"><i class="fas fa-pen-square"></i></a>
                        <a href="{{ path('color_delete', {'id': color.id}) }}" class="text-danger"><i class="fas fa-trash-alt"></i></a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <div class="d-flex justify-content-end my-5">
        <a href="{{ path('color_create') }}" class="btn">Ajouter un produit</a>
    </div>

{% endblock %}