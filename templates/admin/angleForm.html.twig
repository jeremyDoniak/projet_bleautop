{% extends 'baseAdmin.html.twig' %}

{% block title %}angle{% endblock %}

{% block body %}

    {{ form_start(angleForm) }}

        <div class="row">
            <div class="col-12">
                {{ form_row(angleForm.name, {'label': 'Nom'}) }}
            </div>
        </div>

        <div class="col-12 d-flex justify-content-end">
            <a href="{{ path('admin_angle_index') }}" class="btn btn-danger m-3">Annuler</a>
            <button class="btn btn-success m-3">Créer / Modifier</button>
        </div>
        
    {{ form_end(angleForm) }}

{% endblock %}